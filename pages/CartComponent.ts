/**
 * CartComponent
 *
 * Encapsulates interactions and assertions for the application's shopping cart UI within Playwright page objects.
 *
 * A focused component class that builds on BaseComponent to provide cart-specific helpers:
 */

import BaseComponent from "./BaseComponent";

export default class CartComponent extends BaseComponent {}
